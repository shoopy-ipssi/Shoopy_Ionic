import { ScenarioPipe } from '../scenario/scenario.pipe';

describe('ScenarioPipe', () => {
  it('create an instance', () => {
    const pipe = new ScenarioPipe();
    expect(pipe).toBeTruthy();
  });
});
